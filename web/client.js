console.log("client.js started")

class chatClient {
    currentChat;
    constructor(sock){
        this.socket = sock;
    }

    sendChat(text, alpha){
        //!If the chat exists ->
            //! add to that chat
        //!else
            //!Create new Chat
    }

    updateDom(){
        //reCreatethe dom from blank
    }
}